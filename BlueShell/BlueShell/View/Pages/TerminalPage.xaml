<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BlueShell.View.Pages.TerminalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:BlueShell.Converters"
    xmlns:model="using:BlueShell.Model"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:TextTrimmer x:Key="TextTrimmer"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisible"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibleInverted" Invert="True"/>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="TerminalColumnDefinition" Width="2*"/>
            <ColumnDefinition x:Name="DisplayColumnDefinition" Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <RichEditBox x:Name="Terminal"
                         AcceptsReturn="False"
                         FontFamily="Cascadia Code"
                         FontSize="18"
                         Grid.Row="0"
                         IsSpellCheckEnabled="False"
                         PreviewKeyDown="Terminal_PreviewKeyDown"
                         Paste="Terminal_Paste"
                         SelectionChanged="Terminal_SelectionChanged"
                         TextChanging="Terminal_TextChanging">
            </RichEditBox>

            <ScrollViewer x:Name="OutputScrollViewer"
                          Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          VerticalAnchorRatio="1.0">
                <ItemsRepeater x:Name="OutputRepeater">
                    <ItemsRepeater.Layout>
                        <StackLayout Spacing="0"/>
                    </ItemsRepeater.Layout>

                    <DataTemplate x:DataType="model:OutputLine">
                        <Grid>
                            <TextBlock Visibility="{x:Bind IsRich, Converter={StaticResource BoolToVisibleInverted}}"
                                   Text="{x:Bind Text}"
                                   Foreground="{x:Bind Foreground}"
                                   FontFamily="Cascadia Code"
                                   FontSize="18"
                                   IsTextSelectionEnabled="False"
                                   TextWrapping="NoWrap"
                                   TextTrimming="CharacterEllipsis"
                                   MaxWidth="{Binding ElementName=OutputScrollViewer, Path=ViewportWidth}"/>
                            <ItemsControl Visibility="{x:Bind IsRich, Converter={StaticResource BoolToVisible}}"
                                      ItemsSource="{x:Bind Segments}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="model:OutputSegment">
                                        <TextBlock Text="{x:Bind Text}"
                                               Foreground="{x:Bind Color}"
                                               FontStyle="{x:Bind FontStyle}"
                                               FontWeight="{x:Bind FontWeight}"
                                               FontFamily="Cascadia Code"
                                               FontSize="18"
                                               IsTextSelectionEnabled="False"
                                               TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis"
                                               MaxWidth="{Binding ElementName=OutputScrollViewer, Path=ViewportWidth}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater>
            </ScrollViewer>
        </Grid>
        <Grid Grid.Column="1"
              x:Name="DataDisplayGrid">
            <ListView x:Name="DataDisplay"
                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              SelectionMode="Multiple">
                <ListView.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate x:DataType="model:DataDisplayGroup">
                                <TextBlock Text="{x:Bind Header}"
                               FontFamily="Cascadia Mono"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,12,0,6"
                               Opacity="0.85"/>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ListView.GroupStyle>
                <ListView.HeaderTemplate>
                    <DataTemplate x:DataType="model:DataDisplayHeader">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" 
                           Text="{x:Bind NameHeader}"
                           FontFamily="Cascadia Mono"
                           FontSize="14"                                      
                           Margin="{x:Bind NameMargin}"/>
                            <TextBlock Grid.Column="1" 
                           Text="{x:Bind SizeHeader}"
                           FontFamily="Cascadia Mono"
                           FontSize="14"
                           Margin="{x:Bind SizeMargin}"/>
                            <TextBlock Grid.Column="2" 
                           Text="{x:Bind TypeHeader}"
                           FontFamily="Cascadia Mono"
                           FontSize="14"
                           Margin="{x:Bind TypeMargin}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.HeaderTemplate>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="model:DataDisplayItem">
                        <Grid>
                            <Image Source="{x:Bind ItemIcon, Mode=OneWay}" 
                       Width="{x:Bind ImageSize}"        
                       Height="{x:Bind ImageSize}"
                       HorizontalAlignment="Left"
                       Margin="{x:Bind ImageMargin}"/>
                            <TextBlock Text="{x:Bind ItemName, Converter={StaticResource TextTrimmer}, ConverterParameter = 60}"
                           FontFamily="Cascadia Mono"
                           FontSize="14"
                           Padding="{x:Bind TextPadding}"
                           VerticalAlignment="Center"
                           Margin="14, 0, 0, 0"/>
                            <ProgressBar Value="{x:Bind TakenSpace}"
                             Width="260"
                             Minimum="0"
                             Maximum="{x:Bind TotalSize}"
                             Visibility="{x:Bind IsTakenSpaceVisible}"
                             Foreground="{x:Bind Color, Mode=OneWay}"
                             Margin="{x:Bind ProgressMargin}"
                             HorizontalAlignment="Left"/>
                            <TextBlock Text="{x:Bind ItemType}"
                           FontFamily="Cascadia Mono"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Padding="0, 0, 0, 0"
                           Margin="{x:Bind ItemTypeMargin}"/>
                            <TextBlock Text="{x:Bind ItemSize}"
                           FontFamily="Cascadia Mono"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Visibility="{x:Bind IsSizeVisible}"
                           Padding="0, 0, 0, 0"
                           Margin="542, 0, 0, 0"/>
                            <TextBlock Text="{x:Bind ItemSizeType}"
                           FontFamily="Cascadia Mono"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Padding="0, 0, 0, 0"
                           Margin="640, 0, 0, 0"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
